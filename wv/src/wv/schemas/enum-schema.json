{
    "$schema": "http://json-schema.org/schema",
    "id": "WVEnumSchematic",
    "title": "ng new wv-Enum options schema",
    "type": "object",
    "description": "Create a new enum",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the enum",
            "x-prompt": "Name:",
            "$default": {
                "$source": "argv",
                "index": 0
            }
        },
        "path": {
            "type": "string",
            "description": "The path of the enum to be created",
            "$default": {
                "$source": "argv",
                "index": 1
            }
        },
        "project": {
            "type": "string",
            "description": "The project of enum module.",
            "$default": {
              "$source": "projectName"
            }
        }        
    }
}